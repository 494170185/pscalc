"""中文使用说明：快速上手。

# 安装与试用

```bash
pip install -e .
pscalc scenarios              # 看 11 个内置场景
pscalc run substation_110     # 110kV 变电站计算书直接打印
pscalc check chain_110        # 场景自检（加载/拓扑/短路/潮流）
pscalc export hub_220 --format json -o faults.json
```

# 三步跑一个自己的工程

1. 照 `docs/scenario-format.md` 写一个 JSON：
   母线表（含电源）、支路表（线路/主变）、负荷表。
2. `pscalc check 你的场景.json` 确认能算。
3. `pscalc run 你的场景.json -o 计算书.md` 出报告。

# 每个模块单独用（Python）

```python
from pscalc.elements import LineParams
from pscalc.network import Network
from pscalc.perunit import SystemBase

base = SystemBase(100, 115)
net = Network(base)
net.add_bus("A", 110, is_source=True)
net.add_bus("B", 110)
net.add_line("A", "B", LineParams(0.1, 0.4, 20))
print(net.bus_impedance("B"))   # 戴维南等值阻抗（pu）
```

# 模块速查

| 要算什么 | 用哪个 |
|---|---|
| 三相短路 Ik''/ip/Ith | `pscalc.shortcircuit` |
| 单相接地/两相 | `pscalc.unbalanced` |
| 潮流与网损 | `pscalc.powerflow` |
| 电压损耗与调压 | `pscalc.voltagedrop` |
| 电容器补偿 | `pscalc.compensation` |
| 设备校验 | `pscalc.equipment` |
| 接地网 | `pscalc.grounding` |
| 避雷针保护范围 | `pscalc.lightning` |
| 保护整定 | `pscalc.protection` |
| 弧垂与对地距离 | `pscalc.mechanical` |
| 经济截面 | `pscalc.economic` |
| 载流量校正 | `pscalc.ampacity` |
| 负荷统计 | `pscalc.demand` |
| 可靠性指标 | `pscalc.reliability` |

# 常见问题

- **环网会被拒绝吗？** 会。潮流与戴维南等值都按辐射网（树形）
  口径实现，加载场景时 `validate()` 会拦下环网。
- **为什么零依赖？** 工程现场环境经常离线，pip 不可用；
  纯标准库保证拷过去就能跑。
- **数值变了怎么办？** `tests/test_baseline.py` 会失败——
  先解释原因，再 `python tools/update_baseline.py` 重生成基线。
